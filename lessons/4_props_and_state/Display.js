const React = require('react-native');
const {View, Text} = React;

const Display = React.createClass({
    render: function() {
        return (<Text>
            Display
        </Text>);
    }
});
